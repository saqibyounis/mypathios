# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'MyPath' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for MyPath

  pod 'Firebase/Auth'
  pod 'Firebase/Firestore'
  pod 'Firebase/Storage'
  pod 'RealmSwift', '~> 20.0'
  # RxSwift and RxCocoa
  pod 'RxSwift', '~> 6.6'
  pod 'RxCocoa', '~> 6.6'

  # Optional: For UI-Bindings with RxRelay
  pod 'RxRelay', '~> 6.6'

  # Optional: RxSwift extensions (if needed)
  pod 'RxSwiftExt'

  target 'MyPathTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'MyPathUITests' do
    # Pods for testing
  end

end
